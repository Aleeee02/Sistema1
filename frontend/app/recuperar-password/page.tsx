"use client"; import Link from "next/link"; import {FormEvent,useState} from "react";
export default function Page(){const[email,setEmail]=useState(""),[message,setMessage]=useState(""),[token,setToken]=useState("");async function submit(e:FormEvent){e.preventDefault();const r=await fetch("/api/auth/forgot",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const b=await r.json();setMessage(b.mensaje);setToken(b.recovery_token||"")}return <main className="grid min-h-screen place-items-center bg-slate-100 p-4"><form onSubmit={submit} className="w-full max-w-md space-y-4 rounded-2xl bg-white p-7 shadow-xl"><h1 className="text-2xl font-bold">Recuperar contraseña</h1><p className="text-sm text-slate-500">Ingresa el correo asociado a tu cuenta.</p><input required type="email" className="form-control" value={email} onChange={e=>setEmail(e.target.value)} placeholder="correo@empresa.com"/><button className="button primary w-full">Generar enlace</button>{message?<div className="rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{message}</div>:null}{token?<Link className="button w-full" href={`/restablecer-password?token=${encodeURIComponent(token)}`}>Continuar con el enlace de desarrollo</Link>:null}<Link href="/login" className="block text-center text-sm text-blue-600">Volver al inicio de sesión</Link></form></main>}
